<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">
    <select id="usercheck" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE userId = #{userId}
        AND userPassword = #{userPassword}
    </select>

    <select id="getUserInfo" parameterType="map" resultType="mybatis.vo.UserVO">
        SELECT userIdx, userName, userId, userPassword, userEmail, userPhone, userPoint, userGrade, userRegDate, userStatus
        FROM user
        WHERE userId = #{userId}
    </select>

    <!-- 관리자 검증 -->
    <select id="adminCheck" resultType="int">
        SELECT COUNT(*)
        FROM admin
        WHERE adminId = #{adminId}
        AND adminPassword = #{adminPassword}
        AND adminStatus = 0
    </select>

    <!-- 로그인한 관리자 정보 가져오기 -->
    <select id="getAdminInfo" parameterType="map" resultType="mybatis.vo.AdminVO">
        SELECT *
        FROM admin
        WHERE adminId = #{adminId}
    </select>
</mapper>
