<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="logManagement">

  <!-- 로그 조회 -->
  <select id="getAllLogs" resultType="mybatis.vo.LogVO">
    SELECT logIdx, logType, adminIdx, logTarget, logInfo, logPreValue, logCurValue, logDate
    FROM log
    ORDER BY logDate DESC;
  </select>

</mapper>