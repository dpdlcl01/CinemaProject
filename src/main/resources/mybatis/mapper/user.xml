<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
    <insert id="add" parameterType="Map">
        INSERT INTO user(userId, userName, userEmail, userPassword, userPhone, userPoint, userGrade, userRegDate, userStatus)
        VALUES (#{userId}, #{userName}, #{userEmail}, ${userPassword}, ${userPhone}, 0, normal, now(), 1)
    </insert>

</mapper>